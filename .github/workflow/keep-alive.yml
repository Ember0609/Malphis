name: Keep Render Awake

on:
  schedule:
    - cron: "*/14 * * * *" # สั่งให้ทำงานทุกๆ 14 นาที

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render API
        run: curl -s https://malphis.onrender.com/